# Phase 16 Platform Polish

Branch: platform-polish

Scope:

- customer-only public signup
- partner info page
- editable store inventory
- dashboard-safe auth profile throttle
- API docs at /api-docs and /api-docs-json
- generic landing page copy
- footer partner link

CSS dev reset if old Next chunks 404:

```bash
rm -rf apps/admin-dashboard/.next
npm run dev --workspace=apps/admin-dashboard
```

Status: pending proof.
